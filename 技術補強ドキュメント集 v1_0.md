# 技術補強ドキュメント集 v1.0

## エンジニア2名向け実装支援パッケージ

**作成日**：2026年5月19日
**プロジェクト**：対面サービス業向け統合AIプラットフォーム V0

-----

## 目次

- [B1. 共通の型定義(TypeScript types)](#b1-共通の型定義)
- [B2. データベーススキーマSQL完全版](#b2-データベーススキーマsql完全版)
- [B3. 業種別テンプレ140種の具体例](#b3-業種別テンプレ140種の具体例)
- [B4. コンシェルジュのシステムプロンプト4種](#b4-コンシェルジュのシステムプロンプト4種)
- [B5. 既存資産連携の詳細API仕様書](#b5-既存資産連携の詳細api仕様書)
- [B6. テストケース一覧](#b6-テストケース一覧)

-----

# B1. 共通の型定義

ファイル配置：`src/types/api.ts`(エンジニアA・B両方で共有)

```typescript
// ====================================
// 業種・プラン・LLM等の列挙型
// ====================================

export type IndustryL1 = 
  | 'beauty'      // 理美容
  | 'food'        // 飲食
  | 'retail'      // 小売
  | 'health'      // 健康・治療
  | 'education'   // 教育
  | 'medical'     // 医療
  | 'other';      // その他

export type Plan = 
  | 'free'
  | 'basic'
  | 'pro'
  | 'enterprise';

export type Scale = 
  | '1'           // 1名
  | '2-5'         // 2〜5名
  | '6-15'        // 6〜15名
  | '16+';        // 16名以上

export type LLMModel = 
  | 'gpt-mini'      // GPT-5.4 Mini
  | 'claude-haiku'  // Claude Haiku 4.5
  | 'gemini-flash'; // Gemini 1.5 Flash

export type MessageRole = 
  | 'user'
  | 'assistant'
  | 'system';

export type SubscriptionStatus = 
  | 'trialing'
  | 'active'
  | 'past_due'
  | 'canceled'
  | 'incomplete';

export type TemplateCategory = 
  | 'review'      // クチコミ
  | 'sns'         // SNS
  | 'email'       // メール
  | 'menu'        // メニュー
  | 'management'; // 経営

export type ConciergeCategory = 
  | 'industry'    // 業種別
  | 'common';     // 共通(全業種)

// ====================================
// ユーザー関連
// ====================================

export interface User {
  id: string;                    // UUID
  email: string;
  industry_l1: IndustryL1 | null;
  industry_l2: string | null;
  scale: Scale | null;
  features: string | null;
  plan: Plan;
  stripe_customer_id: string | null;
  onboarded: boolean;
  created_at: string;            // ISO8601
  updated_at: string;
}

export interface OnboardingPayload {
  industry_l1: IndustryL1;
  industry_l2: string;
  scale: Scale;
  features?: string;
}

// ====================================
// チャット関連
// ====================================

export interface Chat {
  id: string;
  user_id: string;
  title: string;
  llm_model: LLMModel;
  created_at: string;
  updated_at: string;
}

export interface Message {
  id: string;
  chat_id: string;
  role: MessageRole;
  content: string;
  llm_model: LLMModel | null;
  tokens_used: number | null;
  created_at: string;
}

export interface ChatRequest {
  message: string;
  llm: LLMModel;
  chat_id?: string;
  history?: Message[];
}

export interface ChatStreamChunk {
  type: 'chunk' | 'done' | 'error';
  content?: string;
  chat_id?: string;
  error?: string;
}

// ====================================
// テンプレート関連
// ====================================

export interface TemplateVariable {
  name: string;              // 変数名(snake_case)
  label: string;             // 表示名(日本語)
  placeholder: string;       // 入力例
  required: boolean;
  type: 'text' | 'textarea' | 'select';
  options?: string[];        // typeがselectの場合
}

export interface Template {
  id: string;
  title: string;
  description: string;
  industry: IndustryL1;
  category: TemplateCategory;
  prompt: string;
  variables: TemplateVariable[];
  usage_count: number;
  is_active: boolean;
  created_at: string;
}

export interface TemplateListResponse {
  templates: Template[];
  total: number;
  page: number;
  limit: number;
}

export interface Favorite {
  id: string;
  user_id: string;
  template_id: string;
  created_at: string;
}

// ====================================
// コンシェルジュ関連
// ====================================

export interface Concierge {
  id: string;
  name: string;
  category: ConciergeCategory;
  industry: IndustryL1 | null;
  description: string;
  icon_url: string | null;
  is_active: boolean;
}

export interface ConciergeChatRequest {
  message: string;
  history?: Message[];
}

// ====================================
// 補助金関連
// ====================================

export interface SubsidySearchRequest {
  query: string;
  prefecture?: string;
  city?: string;
  industry?: IndustryL1;
  page?: number;
  limit?: number;
}

export interface SubsidyResult {
  id: string;
  title: string;
  description: string;
  target: string;           // 対象事業者
  max_amount: number;       // 最大金額(円)
  deadline: string | null;  // 申請期限(ISO8601)
  url: string;              // 詳細URL
  tags: string[];
  prefecture: string | null;
  city: string | null;
}

export interface SubsidySearchResponse {
  results: SubsidyResult[];
  total: number;
  page: number;
  limit: number;
}

// ====================================
// 研修動画関連
// ====================================

export interface TrainingVideo {
  id: string;
  title: string;
  description: string;
  thumbnail_url: string;
  duration_minutes: number;
  lms_url: string;
  industry: IndustryL1 | null;
  category: string;
  is_watched: boolean;
}

export interface TrainingVideoListResponse {
  videos: TrainingVideo[];
  total: number;
}

// ====================================
// LINE連携関連
// ====================================

export interface LineConnection {
  user_id: string;
  line_channel_id: string;
  connected_at: string;
  notification_enabled: boolean;
}

export interface LineConnectRequest {
  channel_access_token: string;
  channel_secret: string;
}

// ====================================
// 課金関連
// ====================================

export interface Subscription {
  id: string;
  user_id: string;
  stripe_subscription_id: string;
  plan: Plan;
  status: SubscriptionStatus;
  current_period_start: string;
  current_period_end: string;
  trial_end: string | null;
  cancel_at_period_end: boolean;
}

export interface CheckoutRequest {
  plan: 'basic' | 'pro';
}

export interface CheckoutResponse {
  url: string;             // Stripe Checkoutへのリダイレクト先
}

// ====================================
// ダッシュボード関連
// ====================================

export interface UsageStats {
  total_count: number;
  total_tokens: number;
  total_cost_jpy: number;
}

export interface LLMBreakdown {
  llm_model: LLMModel;
  count: number;
  percentage: number;
}

export interface DailyUsage {
  date: string;            // YYYY-MM-DD
  count: number;
}

export interface RecommendedAction {
  type: 'unwatched_video' | 'unused_template' | 'subsidy_match';
  title: string;
  description: string;
  link: string;
}

export interface DashboardResponse {
  this_month: UsageStats;
  last_month: UsageStats;
  llm_breakdown: LLMBreakdown[];
  daily_usage_last_7_days: DailyUsage[];
  top_templates: Pick<Template, 'id' | 'title' | 'usage_count'>[];
  recommended_actions: RecommendedAction[];
}

// ====================================
// 共通レスポンス型
// ====================================

export interface ApiError {
  error: string;
  message: string;
  details?: Record<string, unknown>;
}

export interface ApiSuccess<T> {
  data: T;
}

// ====================================
// 業種マスタデータ
// ====================================

export const INDUSTRY_DATA: Record<IndustryL1, { label: string; children: string[] }> = {
  beauty: {
    label: '理美容',
    children: ['美容室', '理容室', 'ネイル', 'エステ', 'アイラッシュ', 'まつエク'],
  },
  food: {
    label: '飲食',
    children: ['カフェ', 'レストラン', '居酒屋', 'ベーカリー', 'ラーメン店', '寿司店'],
  },
  retail: {
    label: '小売',
    children: ['個人商店', '専門店', '雑貨店', 'セレクトショップ', 'EC運営'],
  },
  health: {
    label: '健康・治療',
    children: ['整体', '整骨院', 'マッサージ', '鍼灸', 'カイロプラクティック'],
  },
  education: {
    label: '教育',
    children: ['学習塾', '習い事教室', 'フィットネスジム', 'ヨガスタジオ'],
  },
  medical: {
    label: '医療',
    children: ['歯科', 'クリニック', '薬局', '動物病院'],
  },
  other: {
    label: 'その他',
    children: ['不動産', '写真館', '葬儀社', 'クリーニング店', 'その他'],
  },
};
```

-----

# B2. データベーススキーマSQL完全版

ファイル配置：`supabase/migrations/20260520000001_initial_schema.sql`

```sql
-- ====================================
-- 拡張機能の有効化
-- ====================================
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- ====================================
-- 1. usersテーブル(Supabase auth.usersと連動)
-- ====================================
CREATE TABLE public.users (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT UNIQUE NOT NULL,
  industry_l1 TEXT CHECK (industry_l1 IN ('beauty', 'food', 'retail', 'health', 'education', 'medical', 'other')),
  industry_l2 TEXT,
  scale TEXT CHECK (scale IN ('1', '2-5', '6-15', '16+')),
  features TEXT,
  plan TEXT NOT NULL DEFAULT 'free' CHECK (plan IN ('free', 'basic', 'pro', 'enterprise')),
  stripe_customer_id TEXT UNIQUE,
  onboarded BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_users_email ON public.users(email);
CREATE INDEX idx_users_plan ON public.users(plan);

-- ====================================
-- 2. chatsテーブル
-- ====================================
CREATE TABLE public.chats (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  title TEXT NOT NULL DEFAULT '新しい会話',
  llm_model TEXT NOT NULL CHECK (llm_model IN ('gpt-mini', 'claude-haiku', 'gemini-flash')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_chats_user_id_created_at ON public.chats(user_id, created_at DESC);

-- ====================================
-- 3. messagesテーブル
-- ====================================
CREATE TABLE public.messages (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  chat_id UUID NOT NULL REFERENCES public.chats(id) ON DELETE CASCADE,
  role TEXT NOT NULL CHECK (role IN ('user', 'assistant', 'system')),
  content TEXT NOT NULL,
  llm_model TEXT,
  tokens_used INTEGER,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_messages_chat_id_created_at ON public.messages(chat_id, created_at ASC);

-- ====================================
-- 4. templatesテーブル
-- ====================================
CREATE TABLE public.templates (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title TEXT NOT NULL,
  description TEXT NOT NULL,
  industry TEXT NOT NULL CHECK (industry IN ('beauty', 'food', 'retail', 'health', 'education', 'medical', 'other')),
  category TEXT NOT NULL CHECK (category IN ('review', 'sns', 'email', 'menu', 'management')),
  prompt TEXT NOT NULL,
  variables JSONB NOT NULL DEFAULT '[]'::jsonb,
  usage_count INTEGER NOT NULL DEFAULT 0,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_templates_industry_category ON public.templates(industry, category) WHERE is_active = TRUE;
CREATE INDEX idx_templates_usage_count ON public.templates(usage_count DESC) WHERE is_active = TRUE;

-- ====================================
-- 5. favoritesテーブル
-- ====================================
CREATE TABLE public.favorites (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  template_id UUID NOT NULL REFERENCES public.templates(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE(user_id, template_id)
);

CREATE INDEX idx_favorites_user_id ON public.favorites(user_id);

-- ====================================
-- 6. conciergesテーブル
-- ====================================
CREATE TABLE public.concierges (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT NOT NULL,
  category TEXT NOT NULL CHECK (category IN ('industry', 'common')),
  industry TEXT CHECK (industry IN ('beauty', 'food', 'retail', 'health', 'education', 'medical', 'other')),
  description TEXT NOT NULL,
  system_prompt TEXT NOT NULL,
  icon_url TEXT,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_concierges_industry ON public.concierges(industry) WHERE is_active = TRUE;

-- ====================================
-- 7. concierge_chatsテーブル
-- ====================================
CREATE TABLE public.concierge_chats (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  concierge_id UUID NOT NULL REFERENCES public.concierges(id),
  title TEXT NOT NULL DEFAULT 'コンシェルジュとの会話',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_concierge_chats_user_id ON public.concierge_chats(user_id, created_at DESC);

-- ====================================
-- 8. line_connectionsテーブル
-- ====================================
CREATE TABLE public.line_connections (
  user_id UUID PRIMARY KEY REFERENCES public.users(id) ON DELETE CASCADE,
  line_channel_id TEXT NOT NULL,
  line_user_id TEXT,
  access_token_encrypted TEXT NOT NULL,
  channel_secret_encrypted TEXT NOT NULL,
  notification_enabled BOOLEAN NOT NULL DEFAULT TRUE,
  connected_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_line_connections_line_user_id ON public.line_connections(line_user_id);

-- ====================================
-- 9. subscriptionsテーブル
-- ====================================
CREATE TABLE public.subscriptions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  stripe_subscription_id TEXT UNIQUE NOT NULL,
  plan TEXT NOT NULL CHECK (plan IN ('basic', 'pro', 'enterprise')),
  status TEXT NOT NULL CHECK (status IN ('trialing', 'active', 'past_due', 'canceled', 'incomplete')),
  current_period_start TIMESTAMPTZ NOT NULL,
  current_period_end TIMESTAMPTZ NOT NULL,
  trial_end TIMESTAMPTZ,
  cancel_at_period_end BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_subscriptions_user_id ON public.subscriptions(user_id);
CREATE INDEX idx_subscriptions_status ON public.subscriptions(status);

-- ====================================
-- 10. usage_logsテーブル
-- ====================================
CREATE TABLE public.usage_logs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  action TEXT NOT NULL,
  llm_model TEXT,
  tokens_input INTEGER,
  tokens_output INTEGER,
  cost_jpy NUMERIC(10, 4),
  metadata JSONB DEFAULT '{}'::jsonb,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_usage_logs_user_id_created_at ON public.usage_logs(user_id, created_at DESC);
CREATE INDEX idx_usage_logs_action ON public.usage_logs(action);

-- ====================================
-- 11. subsidy_search_logsテーブル(検索履歴)
-- ====================================
CREATE TABLE public.subsidy_search_logs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  query TEXT NOT NULL,
  prefecture TEXT,
  city TEXT,
  industry TEXT,
  result_count INTEGER,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_subsidy_search_logs_user_id ON public.subsidy_search_logs(user_id, created_at DESC);

-- ====================================
-- updated_at自動更新トリガー
-- ====================================
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER update_users_updated_at BEFORE UPDATE ON public.users
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_chats_updated_at BEFORE UPDATE ON public.chats
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_templates_updated_at BEFORE UPDATE ON public.templates
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_concierge_chats_updated_at BEFORE UPDATE ON public.concierge_chats
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
CREATE TRIGGER update_subscriptions_updated_at BEFORE UPDATE ON public.subscriptions
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

-- ====================================
-- 新規ユーザー登録時のusers自動作成
-- ====================================
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.users (id, email)
  VALUES (NEW.id, NEW.email);
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- ====================================
-- Row Level Security(RLS)の有効化と設定
-- ====================================

-- usersテーブル
ALTER TABLE public.users ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own data" ON public.users
  FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Users can update own data" ON public.users
  FOR UPDATE USING (auth.uid() = id);

-- chatsテーブル
ALTER TABLE public.chats ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own chats" ON public.chats
  FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert own chats" ON public.chats
  FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update own chats" ON public.chats
  FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Users can delete own chats" ON public.chats
  FOR DELETE USING (auth.uid() = user_id);

-- messagesテーブル
ALTER TABLE public.messages ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own messages" ON public.messages
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM public.chats
      WHERE chats.id = messages.chat_id AND chats.user_id = auth.uid()
    )
  );
CREATE POLICY "Users can insert own messages" ON public.messages
  FOR INSERT WITH CHECK (
    EXISTS (
      SELECT 1 FROM public.chats
      WHERE chats.id = messages.chat_id AND chats.user_id = auth.uid()
    )
  );

-- templatesテーブル(全認証ユーザーが参照可能)
ALTER TABLE public.templates ENABLE ROW LEVEL SECURITY;

CREATE POLICY "All authenticated users can view active templates" ON public.templates
  FOR SELECT USING (auth.role() = 'authenticated' AND is_active = TRUE);

-- favoritesテーブル
ALTER TABLE public.favorites ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own favorites" ON public.favorites
  FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can manage own favorites" ON public.favorites
  FOR ALL USING (auth.uid() = user_id);

-- conciergesテーブル(全認証ユーザーが参照可能)
ALTER TABLE public.concierges ENABLE ROW LEVEL SECURITY;

CREATE POLICY "All authenticated users can view active concierges" ON public.concierges
  FOR SELECT USING (auth.role() = 'authenticated' AND is_active = TRUE);

-- concierge_chatsテーブル
ALTER TABLE public.concierge_chats ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own concierge chats" ON public.concierge_chats
  FOR ALL USING (auth.uid() = user_id);

-- line_connectionsテーブル
ALTER TABLE public.line_connections ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own line connection" ON public.line_connections
  FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can manage own line connection" ON public.line_connections
  FOR ALL USING (auth.uid() = user_id);

-- subscriptionsテーブル
ALTER TABLE public.subscriptions ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own subscription" ON public.subscriptions
  FOR SELECT USING (auth.uid() = user_id);

-- usage_logsテーブル
ALTER TABLE public.usage_logs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own usage logs" ON public.usage_logs
  FOR SELECT USING (auth.uid() = user_id);

-- subsidy_search_logsテーブル
ALTER TABLE public.subsidy_search_logs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own search logs" ON public.subsidy_search_logs
  FOR SELECT USING (auth.uid() = user_id);

-- ====================================
-- 完了
-- ====================================
```

-----

# B3. 業種別テンプレ140種の具体例

7業種 × 20種 = 140テンプレ。ここでは**理美容業20種＋飲食業20種**を具体例として完全記載し、残り5業種は構造とサンプル数件を提示します。

## 3.1 理美容業 20種

### カテゴリ: クチコミ(review) 4種

#### T-BTY-RV-001：Googleレビュー高評価返信

- **タイトル**: 高評価クチコミへの感謝返信
- **説明**: 4〜5星のレビューに対する温かい返信文を生成
- **variables**:
  - `customer_name` (任意): お客様のお名前(分かる場合)
  - `review_content` (必須): いただいたクチコミの内容
  - `mentioned_staff` (任意): 言及されたスタッフ名
- **prompt**:

```
あなたは美容室の店長です。以下のクチコミに対する返信を書いてください。

【クチコミ内容】
{{review_content}}

{{customer_name}}様への返信として、以下を含めてください:
- 来店への感謝
- クチコミ内容への具体的な反応
{{mentioned_staff}}への言及(あれば)
- 次回来店への期待を込めた一言

文体: 丁寧でありながら親しみのある口調。絵文字は最小限。200文字程度。
```

#### T-BTY-RV-002：低評価クチコミへの誠実な対応

- **variables**: `review_content`(必須), `issue_summary`(任意)
- **prompt**: 1〜2星のレビューに誠実かつ改善姿勢を示す返信を生成。攻撃的にならず、改善への意欲を示す内容。

#### T-BTY-RV-003：HOTPEPPER Beautyレビュー返信

- HOTPEPPER Beauty特有の文体・トーン

#### T-BTY-RV-004：クチコミから改善ポイント抽出

- 複数のクチコミから経営改善のヒントを分析

### カテゴリ: SNS(sns) 6種

#### T-BTY-SN-001：施術前後写真のInstagram投稿

- **variables**: `treatment_type`(必須), `customer_features`(任意), `hashtags_focus`(任意)
- **prompt**:

```
美容室のInstagram投稿文を作成してください。

施術内容: {{treatment_type}}
お客様の特徴・髪質: {{customer_features}}
重視したいハッシュタグの方向性: {{hashtags_focus}}

以下の構成で:
- キャッチーな1行目
- 施術ポイント(2〜3行)
- お客様への一言
- ハッシュタグ20個程度(関連性の高い順)

絵文字は適度に。300〜400文字程度。
```

#### T-BTY-SN-002：カラー紹介投稿

#### T-BTY-SN-003：キャンペーン告知投稿

#### T-BTY-SN-004：スタッフ紹介投稿

#### T-BTY-SN-005：お客様事例(ビフォーアフター)投稿

#### T-BTY-SN-006：トレンド情報の発信投稿

### カテゴリ: メール(email) 3種

#### T-BTY-EM-001：誕生日メッセージ

#### T-BTY-EM-002：来店お礼メール

#### T-BTY-EM-003：久しぶり連絡メール

### カテゴリ: メニュー(menu) 3種

#### T-BTY-MN-001：新メニュー紹介文

#### T-BTY-MN-002：季節メニュー告知

#### T-BTY-MN-003：オプションメニュー追加提案

### カテゴリ: 経営(management) 4種

#### T-BTY-MG-001：労務相談(基礎)

#### T-BTY-MG-002：薬機法チェック

#### T-BTY-MG-003：顧客カルテ要約

#### T-BTY-MG-004：月次売上分析と改善提案

## 3.2 飲食業 20種

### カテゴリ: クチコミ(review) 4種

- T-FOD-RV-001：食べログ高評価返信
- T-FOD-RV-002：食べログ低評価返信
- T-FOD-RV-003：Googleレビュー返信
- T-FOD-RV-004：クチコミ分析と改善提案

### カテゴリ: SNS(sns) 6種

- T-FOD-SN-001：本日のおすすめInstagram投稿
- T-FOD-SN-002：新メニュー紹介投稿
- T-FOD-SN-003：限定告知投稿
- T-FOD-SN-004：店舗の雰囲気紹介
- T-FOD-SN-005：スタッフ紹介
- T-FOD-SN-006：イベント告知

### カテゴリ: メール(email) 3種

- T-FOD-EM-001：予約確認メール
- T-FOD-EM-002：キャンセル対応
- T-FOD-EM-003：予約リマインド

### カテゴリ: メニュー(menu) 3種

- T-FOD-MN-001：メニュー説明文作成
- T-FOD-MN-002：日替わりメニュー告知
- T-FOD-MN-003：写真キャプション

### カテゴリ: 経営(management) 4種

- T-FOD-MG-001：原価計算サポート
- T-FOD-MG-002：メニュー改善提案
- T-FOD-MG-003：衛生管理チェックリスト
- T-FOD-MG-004：シフト管理相談

## 3.3 小売業 20種

各カテゴリ4〜6種(略)。サンプル：

- T-RTL-SN-001：商品紹介Instagram投稿
- T-RTL-EM-001：LINE配信文(セール告知)
- T-RTL-MG-001：在庫管理相談

## 3.4 健康・治療系 20種

サンプル：

- T-HLT-SN-001：施術紹介投稿
- T-HLT-RV-001：患者様のお声への返信
- T-HLT-MG-001：施術メニュー法的注意点

## 3.5 教育系 20種

サンプル：

- T-EDU-SN-001：生徒募集投稿
- T-EDU-EM-001：保護者向けお知らせ
- T-EDU-MG-001：カリキュラム改善

## 3.6 医療系 20種

サンプル：

- T-MED-SN-001：医院紹介投稿
- T-MED-EM-001：患者様向けお知らせ
- T-MED-MG-001：集患戦略相談

## 3.7 その他(汎用) 20種

サンプル：

- T-OTH-EM-001：ビジネスメール(一般)
- T-OTH-MG-001：見積書作成サポート
- T-OTH-SN-001：自社サービス紹介投稿

## 3.8 投入用SQLサンプル

```sql
-- 投入例(理美容業のT-BTY-RV-001)
INSERT INTO public.templates (title, description, industry, category, prompt, variables, is_active)
VALUES (
  '高評価クチコミへの感謝返信',
  '4〜5星のレビューに対する温かい返信文を生成',
  'beauty',
  'review',
  'あなたは美容室の店長です。以下のクチコミに対する返信を書いてください。

【クチコミ内容】
{{review_content}}

{{customer_name}}様への返信として、以下を含めてください:
- 来店への感謝
- クチコミ内容への具体的な反応
{{mentioned_staff}}への言及(あれば)
- 次回来店への期待を込めた一言

文体: 丁寧でありながら親しみのある口調。絵文字は最小限。200文字程度。',
  '[
    {"name": "customer_name", "label": "お客様のお名前", "placeholder": "例: 田中様", "required": false, "type": "text"},
    {"name": "review_content", "label": "いただいたクチコミ", "placeholder": "クチコミの内容をそのまま貼り付け", "required": true, "type": "textarea"},
    {"name": "mentioned_staff", "label": "言及されたスタッフ", "placeholder": "例: 美容師の山田", "required": false, "type": "text"}
  ]'::jsonb,
  TRUE
);
```

> ※残り140種すべての完全なpromptは、既存プロンプトサイトのデータを活用しながら、Claude Codeで一括生成することを推奨します。本ドキュメントは構造とサンプル提供に主眼を置いています。

-----

# B4. コンシェルジュのシステムプロンプト4種

## 4.1 理美容コンシェルジュ

```
あなたは「理美容コンシェルジュ」です。
理美容業界(美容室・理容室・ネイル・エステ・アイラッシュ等)のサロン経営に
20年以上携わってきた経験豊富なアドバイザーとして振る舞ってください。

【あなたの得意分野】
- サロン経営全般(売上向上、客単価アップ、リピーター戦略)
- スタッフ育成・マネジメント
- 集客戦略(SNS、Googleビジネスプロフィール、HOTPEPPER Beauty)
- 予約管理・キャンセル対策
- 薬機法・景表法に基づく広告表現の注意点
- インボイス・電子帳簿保存法など、サロン経営に関連する制度

【ユーザーの背景】
業種: {{industry_l2}}
店舗規模: {{scale}}
店舗の特徴: {{features}}

【回答のスタイル】
- 業界の実情を踏まえた、現場で実践できる具体的なアドバイスを提供してください
- 抽象論ではなく、「明日から何をすればいいか」が明確になる内容を心がけてください
- 必要に応じて、複数の選択肢を提示し、それぞれのメリット・デメリットを示してください
- 200〜500文字程度を基本とし、長くなりすぎないようにしてください

【重要な制約】
- 法律・税務・労務に関する具体的判断は、必ず専門家(弁護士・税理士・社労士)への
  相談を推奨してください
- 薬機法に関する広告表現は、保守的に判断してください
- 医療行為に該当する可能性のある内容は、医師への相談を案内してください
- 回答の最後に必要に応じて「ご状況に応じた詳細なアドバイスをご希望の方は、
  電話サポートまでお問い合わせください」と案内してください
```

## 4.2 飲食コンシェルジュ

```
あなたは「飲食コンシェルジュ」です。
飲食業界(レストラン・カフェ・居酒屋・ベーカリー等)の店舗経営に
20年以上携わってきた経験豊富なアドバイザーとして振る舞ってください。

【あなたの得意分野】
- 飲食店経営全般(売上向上、原価管理、客単価アップ)
- メニュー戦略・原価計算
- HACCP対応・衛生管理
- 集客戦略(食べログ、Googleレビュー、Instagram)
- 予約管理・ノーショー対策
- スタッフ管理・シフト最適化
- インボイス・軽減税率など、飲食店に関連する税制

【ユーザーの背景】
業種: {{industry_l2}}
店舗規模: {{scale}}
店舗の特徴: {{features}}

【回答のスタイル】
- 業界の実情(原価率30%以下を目指す等)を踏まえた具体的なアドバイスを提供
- 「明日から実践できる」を最優先
- 数値や指標を活用した分析的なアドバイス
- 200〜500文字程度

【重要な制約】
- 食品衛生・食中毒関連は保健所への確認を必ず推奨
- 法律・税務・労務は専門家への相談を推奨
- 酒類販売関連は所轄税務署への確認を推奨
```

## 4.3 小売コンシェルジュ

```
あなたは「小売コンシェルジュ」です。
小売業(個人商店・専門店・雑貨店・EC運営)の経営に詳しい
経験豊富なアドバイザーとして振る舞ってください。

【あなたの得意分野】
- 店舗運営全般(売上向上、客単価アップ、リピーター戦略)
- 商品仕入れ・在庫管理・棚卸し
- 陳列・VMD(ビジュアルマーチャンダイジング)
- インバウンド対応(免税・多言語)
- EC化・オムニチャネル戦略
- 集客戦略(Instagram、LINE、Googleビジネスプロフィール)
- インボイス・電子帳簿保存法

【ユーザーの背景】
業種: {{industry_l2}}
店舗規模: {{scale}}
店舗の特徴: {{features}}

【回答のスタイル】
- 小売特有の指標(粗利率、回転率、客単価)を意識した分析的アドバイス
- 季節性・トレンドを踏まえた提案
- 200〜500文字程度

【重要な制約】
- 法律・税務・労務は専門家への相談を推奨
- 食品・化粧品等の取り扱いは薬機法・食品衛生法に注意
- 越境ECは関税・各国法令への確認を案内
```

## 4.4 助成金コンシェルジュ(共通・全業種対応)

```
あなたは「助成金コンシェルジュ」です。
中小企業・個人事業主向けの助成金・補助金に詳しい専門アドバイザーとして
振る舞ってください。社会保険労務士事務所と連携し、最新の制度情報に基づいた
正確で実践的なアドバイスを提供します。

【あなたの得意分野】
- 中小企業向けの助成金・補助金の全般的な知識
- 業種別の活用可能制度の提案
- 申請に必要な書類・要件の概要説明
- 採択のためのポイント

【主な対象制度の例】
- 人材開発支援助成金(DX対応コース等)
- IT導入補助金
- 小規模事業者持続化補助金
- 業務改善助成金
- 雇用調整助成金
- キャリアアップ助成金
- 事業再構築補助金
- ものづくり補助金
- その他、地域別の補助金

【ユーザーの背景】
業種: {{industry_l2}}
店舗規模: {{scale}}
所在地: {{prefecture}} {{city}}

【回答のスタイル】
- ユーザーの業種・規模・地域に基づいて、活用可能な助成金を絞り込んで提案
- 各助成金の「対象事業者」「補助上限額」「申請期限」「採択率の傾向」を含める
- 申請ステップを簡潔に説明
- 200〜500文字程度

【絶対的な制約】
- 最終的な制度内容・採択可否は、必ず以下の案内を含めてください:
  「最新の制度内容・申請可否は予告なく変更される場合があります。
   詳細は弊社提携の社労士事務所による無料相談をご利用ください。
   お問い合わせ: [電話番号]」
- 確実性のない情報は「目安として」と前置きする
- 採択を保証する表現は絶対に使わない
- 制度の詳細は厚生労働省・経済産業省・各自治体の公式情報を確認するよう促す

【回答の最後に必ず追加】
「ご状況に応じた具体的な申請サポートをご希望の方は、提携社労士事務所まで
お気軽にお問い合わせください。」
```

## 4.5 投入用SQLサンプル

```sql
INSERT INTO public.concierges (name, category, industry, description, system_prompt, is_active)
VALUES
  ('理美容コンシェルジュ', 'industry', 'beauty', 'サロン経営・薬機法・予約管理・リピーター戦略のプロ', '[上記4.1の全文]', TRUE),
  ('飲食コンシェルジュ', 'industry', 'food', '飲食店経営・HACCP・メニュー戦略・衛生管理のプロ', '[上記4.2の全文]', TRUE),
  ('小売コンシェルジュ', 'industry', 'retail', '店舗運営・在庫管理・陳列・インバウンド対応のプロ', '[上記4.3の全文]', TRUE),
  ('助成金コンシェルジュ', 'common', NULL, '助成金・補助金の専門家(社労士事務所連携)', '[上記4.4の全文]', TRUE);
```

-----

# B5. 既存資産連携の詳細API仕様書

## 5.1 補助金RAG連携

### 5.1.1 確認すべき事項(Day 1で実施)

既存補助金RAG担当者へ以下を確認:

1. **エンドポイントURL**: 本番・開発それぞれのURL
1. **認証方式**: APIキー / OAuth / Bearer Token
1. **レート制限**: 秒間/分間のリクエスト上限
1. **レスポンス時間**: 95%tileのレイテンシ目安
1. **データ更新頻度**: いつ・どのように更新されるか
1. **対応する検索パラメータ**:
- クエリ(自然言語)
- 地域(都道府県・市区町村)
- 業種
- 補助金種別
- 金額レンジ
- 締切期間
1. **レスポンス形式**: JSON構造のサンプル

### 5.1.2 期待するAPI仕様(仮案・要調整)

```
POST {SUBSIDY_RAG_API_URL}/search
Headers:
  Authorization: Bearer {API_KEY}
  Content-Type: application/json
Body:
{
  "query": "美容室向けDX補助金",
  "prefecture": "東京都",
  "city": "渋谷区",
  "industry": "beauty",
  "max_results": 20
}

Response 200:
{
  "results": [
    {
      "id": "sub_xxx",
      "title": "IT導入補助金2026",
      "description": "...",
      "target": "中小企業・小規模事業者",
      "max_amount": 4500000,
      "deadline": "2026-09-30",
      "url": "https://...",
      "tags": ["IT", "DX", "デジタル化"],
      "prefecture": "全国",
      "city": null,
      "relevance_score": 0.92
    }
  ],
  "total": 15,
  "query_time_ms": 850
}
```

### 5.1.3 ラッパー関数の実装方針

`src/lib/api/subsidy.ts`:

```typescript
import type { SubsidySearchRequest, SubsidySearchResponse } from '@/types/api';

const RAG_API_URL = process.env.SUBSIDY_RAG_API_URL!;
const RAG_API_KEY = process.env.SUBSIDY_RAG_API_KEY!;
const CACHE_TTL_MS = 5 * 60 * 1000; // 5分

const cache = new Map<string, { data: SubsidySearchResponse; expires: number }>();

export async function searchSubsidies(params: SubsidySearchRequest): Promise<SubsidySearchResponse> {
  const cacheKey = JSON.stringify(params);
  const cached = cache.get(cacheKey);
  if (cached && cached.expires > Date.now()) {
    return cached.data;
  }

  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(), 10000); // 10秒タイムアウト

  try {
    const res = await fetch(`${RAG_API_URL}/search`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${RAG_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(params),
      signal: controller.signal,
    });

    if (!res.ok) throw new Error(`RAG API error: ${res.status}`);

    const data = await res.json();
    cache.set(cacheKey, { data, expires: Date.now() + CACHE_TTL_MS });
    return data;
  } finally {
    clearTimeout(timeout);
  }
}
```

## 5.2 LMS連携

### 5.2.1 確認すべき事項

1. **LMS製品名**(Teachable / Thinkific / Moodle / 独自開発 等)
1. **API提供有無**
1. **SSO対応**(SAML / OIDC / カスタム)
1. **動画一覧取得API**の有無
1. **視聴履歴取得API**の有無
1. **本サービスからLMSへの遷移URL形式**

### 5.2.2 V0での連携方針(リンク方式)

API連携が複雑な場合、V0では**LMSへの単純リンク**で代用:

```typescript
// src/lib/api/lms.ts (V0版)

export interface TrainingVideo {
  id: string;
  title: string;
  description: string;
  thumbnail_url: string;
  duration_minutes: number;
  lms_url: string;
  industry: IndustryL1 | null;
  category: string;
}

// V0ではハードコードまたは管理画面で管理するメタデータ
const VIDEOS_METADATA: TrainingVideo[] = [
  {
    id: 'video_001',
    title: 'AI活用基礎: ChatGPTの使い方',
    description: '初心者向けの基礎',
    thumbnail_url: 'https://.../thumb_001.jpg',
    duration_minutes: 12,
    lms_url: 'https://[LMS_URL]/courses/ai-basic',
    industry: null, // 共通
    category: 'basic',
  },
  // ...省略
];

export function getTrainingVideos(industry?: IndustryL1, category?: string): TrainingVideo[] {
  return VIDEOS_METADATA.filter(v => 
    (industry === undefined || v.industry === null || v.industry === industry) &&
    (category === undefined || v.category === category)
  );
}
```

V1でAPI連携に移行する想定。

## 5.3 既存助成金AI運用との連携

### 5.3.1 確認すべき事項

1. 既存AI運用の処理フロー
1. APIエンドポイントの有無(or 別途設計が必要か)
1. 入出力形式
1. SLA(レスポンス時間)
1. ユーザー識別の方法

### 5.3.2 統合アプローチ案

|案|内容                       |工数     |
|-|-------------------------|-------|
|A|既存AIをラップしたAPIエンドポイント実装   |中(3〜5日)|
|B|既存AIの呼び出し関数を直接利用         |小(1〜2日)|
|C|V0では助成金コンシェルジュで代替、V1で本格統合|最小(0日) |

**V0推奨**: 案C(コンシェルジュで代替)

## 5.4 既存プロンプトサイトとの連携

### 5.4.1 データ移行プロセス

1. **データエクスポート受領**:
- 形式: CSV または JSON
- 必須項目: タイトル、本文、想定業種、想定カテゴリ
1. **業種・カテゴリ正規化**: 既存タグを本サービスの分類体系にマッピング
1. **variables抽出**: プロンプト中の `{xxx}` を自動検出して `variables` JSONBに変換
1. **Supabase投入**: 一括INSERT

### 5.4.2 移行スクリプト概要

```typescript
// scripts/migrate-prompts.ts

import { createClient } from '@supabase/supabase-js';
import { parse } from 'csv-parse/sync';
import * as fs from 'fs';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY! // RLSをバイパスするため
);

// CSVの想定構造
interface CsvRow {
  title: string;
  description: string;
  prompt: string;
  industry: string; // 既存サイトの業種カラム
  category: string; // 既存サイトのカテゴリカラム
}

// 業種マッピング(既存サイト → 本サービス)
const INDUSTRY_MAP: Record<string, string> = {
  '美容室': 'beauty',
  '理容室': 'beauty',
  'サロン': 'beauty',
  '飲食店': 'food',
  // ...
};

// 変数抽出(例: {店名} → name="store_name")
function extractVariables(prompt: string): Array<{name: string; label: string; placeholder: string; required: boolean; type: string}> {
  const matches = prompt.matchAll(/\{(.+?)\}/g);
  const variables: any[] = [];
  for (const m of matches) {
    const label = m[1];
    variables.push({
      name: `var_${label.replace(/[^a-zA-Z0-9]/g, '_')}`,
      label: label,
      placeholder: `例: ${label}`,
      required: true,
      type: 'text',
    });
  }
  return variables;
}

async function main(csvPath: string) {
  const csv = fs.readFileSync(csvPath);
  const rows: CsvRow[] = parse(csv, { columns: true, skip_empty_lines: true });
  
  const records = rows.map(row => ({
    title: row.title,
    description: row.description,
    industry: INDUSTRY_MAP[row.industry] || 'other',
    category: row.category || 'management',
    prompt: row.prompt.replace(/\{(.+?)\}/g, (_, p) => `{{var_${p.replace(/[^a-zA-Z0-9]/g, '_')}}}`),
    variables: extractVariables(row.prompt),
    is_active: true,
  }));
  
  const { data, error } = await supabase.from('templates').insert(records);
  if (error) console.error('Error:', error);
  else console.log(`Inserted ${records.length} templates`);
}

main(process.argv[2]);
```

実行: `npx tsx scripts/migrate-prompts.ts ./data/prompts.csv`

-----

# B6. テストケース一覧

## 6.1 認証関連

|ID      |テストケース            |期待結果                    |
|--------|------------------|------------------------|
|AUTH-001|メールアドレス+パスワードで新規登録|確認メール送信                 |
|AUTH-002|メール認証リンククリック      |usersテーブルに自動INSERT      |
|AUTH-003|正しい認証情報でログイン      |/homeへリダイレクト            |
|AUTH-004|誤ったパスワードでログイン     |エラー表示                   |
|AUTH-005|LINEログイン          |LINE認証画面→コールバック→ログイン成功  |
|AUTH-006|Googleログイン        |Google認証画面→コールバック→ログイン成功|
|AUTH-007|パスワード再発行          |リセットメール送信               |
|AUTH-008|5回連続パスワード誤りでロック   |アカウントロック15分             |
|AUTH-009|認証なしで /chat にアクセス |/loginへリダイレクト           |
|AUTH-010|ログアウト             |セッション削除、/loginへリダイレクト   |

## 6.2 オンボーディング

|ID     |テストケース              |期待結果              |
|-------|--------------------|------------------|
|ONB-001|初回ログイン時にオンボーディング画面表示|ウィザード開始           |
|ONB-002|業種大分類選択 → 中分類が動的表示  |各業種に対応した中分類       |
|ONB-003|必須項目未入力時に「次へ」非活性    |ボタンが押せない          |
|ONB-004|オンボーディング完了          |usersテーブル更新、/homeへ|
|ONB-005|オンボーディング済みユーザーはスキップ |/homeへ直接アクセス可     |

## 6.3 チャット機能

|ID      |テストケース             |期待結果       |
|--------|-------------------|-----------|
|CHAT-001|ChatGPTで質問送信       |ストリーミング応答  |
|CHAT-002|Claudeで質問送信        |ストリーミング応答  |
|CHAT-003|Geminiで質問送信        |ストリーミング応答  |
|CHAT-004|LLM切替後の対話継続        |コンテキスト維持   |
|CHAT-005|システムプロンプトに業種情報自動付加 |業種を意識した回答  |
|CHAT-006|マークダウン形式の応答        |適切に表示      |
|CHAT-007|コードブロックのシンタックスハイライト|言語別に色分け    |
|CHAT-008|チャット履歴の保存          |DBに永続化     |
|CHAT-009|チャット履歴の閲覧          |サイドバーに一覧   |
|CHAT-010|チャット削除             |DBから削除     |
|CHAT-011|月間トークン上限到達         |429エラー、上限案内|
|CHAT-012|LLM API失敗時のフォールバック |別LLMへ自動切替  |

## 6.4 テンプレート機能

|ID     |テストケース         |期待結果               |
|-------|---------------|-------------------|
|TPL-001|業種別テンプレ一覧表示    |自業種のテンプレのみ         |
|TPL-002|カテゴリフィルタ       |該当カテゴリのみ           |
|TPL-003|検索機能           |キーワードマッチ           |
|TPL-004|テンプレ詳細表示       |variablesに応じた入力フォーム|
|TPL-005|テンプレ実行 → チャット遷移|プロンプト事前セット         |
|TPL-006|お気に入り追加        |favoritesに登録       |
|TPL-007|お気に入り解除        |favoritesから削除      |
|TPL-008|使用回数カウントアップ    |usage_count + 1    |

## 6.5 補助金検索

|ID     |テストケース             |期待結果            |
|-------|-------------------|----------------|
|SUB-001|自然言語検索             |関連補助金一覧         |
|SUB-002|地域絞り込み             |該当地域のみ          |
|SUB-003|レスポンス時間3秒以内        |性能要件達成          |
|SUB-004|0件時の空状態表示          |適切なメッセージ        |
|SUB-005|RAG API障害時のフォールバック |エラー表示+お問合せ誘導    |
|SUB-006|「相談する」→ 助成金コンシェルジュへ|コンシェルジュ画面で文脈引き継ぎ|

## 6.6 研修動画

|ID     |テストケース        |期待結果    |
|-------|--------------|--------|
|TRN-001|業種別動画一覧       |自業種関連動画 |
|TRN-002|カテゴリタブ切替      |該当カテゴリ動画|
|TRN-003|動画クリック → LMS遷移|新規タブで開く |

## 6.7 コンシェルジュ

|ID     |テストケース             |期待結果      |
|-------|-------------------|----------|
|CNC-001|コンシェルジュ一覧表示        |4種が表示     |
|CNC-002|理美容コンシェルジュとの対話     |業界専門回答    |
|CNC-003|飲食コンシェルジュとの対話      |業界専門回答    |
|CNC-004|助成金コンシェルジュとの対話     |助成金提案+免責表示|
|CNC-005|業種不一致のコンシェルジュへのアクセス|403または推奨表示|
|CNC-006|免責事項の表示            |各回答末尾に表示  |

## 6.8 LINE連携

|ID      |テストケース           |期待結果               |
|--------|-----------------|-------------------|
|LINE-001|LINE公式アカウント連携    |line_connectionsに保存|
|LINE-002|LINE Webhookで質問受信|AI回答をLINE返信        |
|LINE-003|LINE Webhook署名検証 |不正リクエスト拒否          |
|LINE-004|LINE連携解除         |レコード削除             |

## 6.9 ダッシュボード

|ID     |テストケース     |期待結果      |
|-------|-----------|----------|
|DSH-001|今月の利用統計表示  |正確な集計     |
|DSH-002|過去7日のグラフ   |日別集計表示    |
|DSH-003|LLM内訳の円グラフ |比率正確      |
|DSH-004|おすすめアクション表示|個別ユーザー向け推奨|

## 6.10 課金

|ID     |テストケース             |期待結果                     |
|-------|-------------------|-------------------------|
|BIL-001|ベーシックプラン選択         |Stripe Checkoutへ         |
|BIL-002|プロプラン選択            |Stripe Checkoutへ         |
|BIL-003|初月無料(30日トライアル)     |trial_endが30日後           |
|BIL-004|トライアル終了で自動課金       |subscription更新           |
|BIL-005|課金成功時のplan自動アップグレード|users.planが更新            |
|BIL-006|プラン変更              |Stripe Customer Portal経由 |
|BIL-007|解約                 |cancel_at_period_end=true|
|BIL-008|解約後の機能制限           |freeプランへ降格               |
|BIL-009|Webhook署名検証        |不正リクエスト拒否                |

## 6.11 セキュリティ

|ID     |テストケース            |期待結果            |
|-------|------------------|----------------|
|SEC-001|他人のチャット履歴アクセス試行   |403または空配列       |
|SEC-002|RLS有効化確認          |全テーブルでON        |
|SEC-003|SQLインジェクション試行     |防御される           |
|SEC-004|XSS試行             |エスケープされる        |
|SEC-005|APIキーのクライアント露出チェック|ブラウザDevToolsに出ない|
|SEC-006|レート制限             |上限超過で429        |
|SEC-007|パスワード総当たり         |5回失敗でロック        |

## 6.12 性能

|ID     |テストケース       |期待結果        |
|-------|-------------|------------|
|PRF-001|ログインレスポンス    |1.5秒以内      |
|PRF-002|チャット初動応答     |2秒以内        |
|PRF-003|補助金検索        |3秒以内        |
|PRF-004|1,000同時接続    |全機能正常動作     |
|PRF-005|Lighthouseスコア|パフォーマンス70点以上|

## 6.13 受け入れテスト(UAT)

|ID     |テストケース                   |担当    |
|-------|-------------------------|------|
|UAT-001|新電力営業1名が15分で全機能を一通り使える   |経営者+営業|
|UAT-002|既存研修受講者3名が業種別テンプレを実用できる  |研修担当  |
|UAT-003|コールセンタースタッフが擬似問い合わせに回答できる|CS担当  |
|UAT-004|関連会社経営者がデモを実施できる         |関連会社  |

-----

**ドキュメント終了**

このドキュメントは技術実装の核となる6種類の補強資料を含んでいます。
エンジニアA・B、運用担当者、経営者がそれぞれ必要な箇所を参照してください。