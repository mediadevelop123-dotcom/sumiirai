# テスト版実行計画 v1.0

## 対面サービス業向け統合AIプラットフォーム V0 — テスト版(本実装の前段)

**作成日**:2026年5月20日
**対象期間**:18日(キックオフから完成まで)
**位置づけ**:本実装(『1ヶ月リリース実行計画書 v2_0.md』に基づく30日リリース)の**前段で、UX検証と実装難所の早期発見を目的としたテスト版**

-----

## 0. このドキュメントの目的と読者

### 0.1 目的

- 本実装に着手する前に、コア体験を実際に動かして検証する
- 実装上の難所(SSEストリーミング・自前RAG構築・認証フロー)を早めに潰す
- テスト版の運用フィードバックを本実装の仕様に反映する

### 0.2 想定読者

- エンジニアA(フロントエンド担当)
- エンジニアB(バックエンド・データ・API担当)
- 経営者(意思決定・データ収集サポート)
- 関連会社・既存資産担当者(状況把握のため)
- 後から参加する人(全体像と判断経緯の把握のため)

### 0.3 関連ドキュメント

| 文書 | 関係 |
|--|--|
| 『1ヶ月リリース実行計画書 v2_0.md』 | 本実装の親計画書。本ドキュメントはこの前段 |
| 『エンジニアA フロントエンド担当 仕様書兼指示書.md』 | 本実装の詳細仕様。テスト版はこの一部を先取り実装 |
| 『エンジニアB バックエンド担当 仕様書兼指示書.md』 | 同上 |
| 『技術補強ドキュメント集 v1_0.md』 | 型定義・DBスキーマ・テストケース等の補強資料 |

-----

## 1. 経緯(なぜテスト版を作るのか)

### 1.1 元計画書の課題

『1ヶ月リリース実行計画書 v2.0』では以下を**30日で全実装**する計画だった:

- 認証(メール/LINE/Google)、業種選択7業種、マルチLLM対話(ChatGPT/Claude/Gemini)
- テンプレ140種、補助金RAG連携、LMS連携、LINE Bot、Stripe課金
- 業種別コンシェルジュ4種、ダッシュボード
- ランディングページ、プレスリリース、β50〜100社案内

### 1.2 実現性の評価

エンジニア2名・実働20日強でこの量は、常識的な見積もりを大きく超える。主な懸念:

1. **ボリュームに対して人員が薄い**:1機能あたり1〜3日が普通の規模が、ほぼ1日刻みで並んでいる
2. **既存資産APIの仕様が未確定なままDay1スタート**:補助金RAG・LMS・既存助成金AI・プロンプトサイトの仕様確認がDay1作業に含まれる
3. **テンプレ140種の品質保証が未設計**:業界特有の表現・薬機法・景表法のチェック工数が見えていない
4. **テスト・UATが後ろに寄りすぎ**:本格UATがDay27の「内部リハーサル1日」だけ
5. **コンプライアンス系が薄い**:利用規約・PP・特商法表記がDay25 LP完成と並列
6. **運用周りの抜け**:Stripe Webhook冪等性、レート制限の月次集計クエリ性能、LiteLLM障害時の挙動等

### 1.3 「補助金RAG」の前提変更(重要)

元計画書では「**既存資産**」として扱われていた補助金RAGだが、議論の結果**実態は未着手で、1から作る必要があることが判明**。これにより:

- 既存API接続のテストではなく、データ収集・ベクトルDB構築から始める必要がある
- 計画書全体の「既存資産活用」前提が部分的に崩れる
- 補助金RAGの構築自体に14〜24日の工数が想定される

### 1.4 方針転換

「全機能を30日で出す」を諦め、以下の二段階に分割:

1. **テスト版(18日)**:コアの5機能だけを実装、社内で動かしてフィードバック取得
2. **本実装**:テスト版の知見を反映して本格開発(別途計画)

-----

## 2. テスト版のスコープ

### 2.1 実装する機能(5つに絞る)

| # | 機能 | 内容 | 備考 |
|---|--|--|--|
| 1 | **メール認証** | signup / login / logout | LINE/Googleは本実装で |
| 2 | **AIチャット** | OpenAI GPT-4o-mini、SSEストリーミング、飲食店向けシステムプロンプト固定 | 業種は飲食固定、選択UIなし |
| 3 | **チャット履歴** | サイドバーに過去会話一覧、クリックで再表示、削除可 | 「使えるサービス」の最低ライン |
| 4 | **補助金検索(自前RAG)** | 飲食特化・30〜50件・pgvectorベクトル検索 | テスト版のコア検証対象 |
| 5 | **補助金→相談導線** | 検索結果の「相談する」ボタンで、選んだ補助金情報をプリセットしたチャットを開始 | 既存資産(RAG)とAI対話の組み合わせ価値を検証 |

画面数:**4画面**(ログイン / サインアップ / チャット / 補助金検索)

### 2.2 削除した機能と理由

| 削除した機能 | 削除理由 |
|--|--|
| Google/LINEログイン | メール認証だけで4名のテスト用途には十分。OAuth設定の手間に対して検証価値が薄い |
| 業種選択UI(7業種ウィザード) | テスト版は飲食固定。業種文脈の効果は飲食1業種で十分検証可能 |
| マルチLLM(3社) | 1社で品質確認すれば十分。切替UIは本実装で |
| テンプレ140種 | チャット+RAGで「もう一人のスタッフ」感は検証できる。テンプレの価値はチャット動いてから検証 |
| LMS連携 | テスト版の目的(コア体験検証)に寄与しない |
| コンシェルジュ4種 | システムプロンプト差し替えだけなので、本実装で着手すれば早い |
| LINE Bot | テスト版で動かしても得られる学びが薄い |
| Stripe課金 | テスト版は無料前提 |
| ダッシュボード | 利用統計は管理側でDB直接見ればよい |
| 利用規約・PP正式版 | β社内向けは口頭合意で代用、本実装前に弁護士レビュー |
| エラー監視(Sentry) | console + Vercel Logsでしのぐ |
| レート制限 | 閉じた4名利用想定なら不要 |

### 2.3 削らないもの(コアとして残す)

| 残すもの | 残す理由 |
|--|--|
| SSEストリーミング | UX体験差が大きすぎる。後付けは難しい |
| pgvectorによるベクトル検索 | RAGの本質。全文検索だと品質が出ない |
| チャット履歴 | これがないと「使えるサービス」にならない |
| RLS(Row Level Security) | 後付けが面倒。最初から入れる |
| Supabase Auth | テスト版・本実装ともに同じ仕組みで進めるため |

-----

## 3. スケジュール(18日)

### 3.1 全体俯瞰

| 期間 | 主テーマ | 完了判定 |
|--|--|--|
| Day 1〜2 | 準備・調査・データ収集着手 | 環境構築完了、データ収集進行中 |
| Day 3〜6 | 基盤実装 | 認証フロー動作、DB完成 |
| Day 7〜10 | コア機能(チャット+RAG) | チャット動作、RAGデータ投入完了 |
| Day 11〜13 | 補助金検索画面+導線 | 補助金検索→チャット遷移が動く |
| Day 14〜16 | 統合テスト・調整 | 全フローが通る、検索精度OK |
| Day 17〜18 | 仕上げ・社内デモ準備 | テストユーザー受け入れ可能 |

### 3.2 詳細(エンジニア2名並行)

#### Day 1〜2:準備・調査
- **共同**:キックオフMTG(1時間)、スコープ・分担最終確認
- **共同**:Supabase/Vercelプロジェクト立ち上げ(ap-northeast-1リージョン)
- **エンジニアA**:Next.js 14プロジェクト雛形+UIコンポーネント基礎+Tailwind設定
- **エンジニアB**:DB設計開始(users / chats / messages / subsidies / subsidy_embeddings)、pgvector拡張の有効化検証
- **経営者(並行)**:補助金データ収集着手(飲食×全国〜東京、30〜50件目標)

#### Day 3〜6:基盤実装
- **エンジニアA**:認証画面(login/signup/logout)、チャットUI骨組み
- **エンジニアB**:Supabase Auth設定、認証API、RLSポリシー、チャットAPI骨組み
- **経営者(並行)**:補助金データ収集継続、エンジニアBへ受け渡し準備

#### Day 7〜10:コア機能
- **エンジニアA**:チャットUI完成(SSEストリーミング・履歴サイドバー・マークダウン表示)
- **エンジニアB**:
  - チャットAPI完成(OpenAI GPT-4o-mini、システムプロンプト「飲食店向け」固定、SSE)
  - 補助金データ構造化、OpenAI Embeddings APIで埋め込み生成
  - pgvectorに投入、検索APIプロトタイプ

#### Day 11〜13:補助金検索画面+導線
- **エンジニアA**:補助金検索画面(検索フォーム / 結果カード / 「相談する」ボタン)
- **エンジニアB**:検索APIブラッシュアップ、「相談する」用にチャットへ補助金情報を渡す処理、検索精度調整

#### Day 14〜16:統合テスト・調整
- **両者**:結合テスト
- **両者**:検索クエリ品質検証(「飲食店 IT導入」「コロナ対策」「人手不足」等)
- バグ修正、UI調整

#### Day 17〜18:仕上げ・社内デモ準備
- レイアウト調整、エラー処理整備
- デモ用ユーザー作成(4名分)
- フィードバック収集の準備(Googleフォーム等)

-----

## 4. 技術スタック

| カテゴリ | 採用 | 備考 |
|--|--|--|
| フロントエンド | Next.js 14 (App Router) + React + TypeScript | 本実装と同じ |
| スタイリング | Tailwind CSS | 本実装と同じ |
| 認証 | Supabase Auth(メールのみ) | LINE/Googleは本実装で追加 |
| DB | Supabase PostgreSQL + pgvector | テスト版から本実装まで継続利用 |
| LLM | OpenAI GPT-4o-mini | 1社固定。マルチLLMは本実装で |
| 埋め込みモデル | OpenAI text-embedding-3-small | 安価で十分な精度 |
| ストリーミング | Server-Sent Events (SSE) | Vercel API Routes対応 |
| デプロイ | Vercel | テスト版・本実装で同じ |
| エラー監視 | console + Vercel Logs | 本実装でSentry追加 |

### 採用しないもの(本実装で追加検討)
- LiteLLM(マルチLLM抽象化レイヤー)
- Sentry / Vercel Analytics
- Stripe SDK
- LINE Bot SDK
- Cloudflare R2 / Vercel KV

-----

## 5. データベース最小構成

### 5.1 必要なテーブル(6つ)

| テーブル | 用途 |
|--|--|
| users | ユーザー基本情報(Supabase Authと連動) |
| chats | チャットセッション |
| messages | 個別メッセージ |
| subsidies | 補助金マスタ(タイトル/対象/金額/締切/URL/タグ等) |
| subsidy_embeddings | 補助金のベクトル埋め込み(pgvector) |
| usage_logs | 利用ログ(原価集計用、最低限) |

### 5.2 本実装との関係

本実装では以下のテーブルが追加される予定(『技術補強ドキュメント集 B2』参照):
- templates / favorites / concierges / concierge_chats / line_connections / subscriptions / subsidy_search_logs

テスト版のテーブル設計は、**本実装のサブセット**になるよう命名・カラム構造を揃える(マイグレーション無しで本実装に移行できるように)。

-----

## 6. 補助金RAGのスコープ詳細

### 6.1 データ収集

| 項目 | 仕様 |
|--|--|
| 対象業種 | 飲食 |
| 対象地域 | 全国 + 東京都(地域絞り込み機能あり) |
| 件数 | 30〜50件(募集中のもの中心) |
| 主なデータソース | jGrants(中企庁)、東京都産業労働局、厚労省人材開発支援助成金、各種補助金ポータル(公開情報) |
| 収集方法 | **手動でCSV作成**(スクレイピングは法務リスクのため避ける) |
| 必須項目 | id / title / description / target(対象事業者) / max_amount / deadline / url / tags / prefecture |

### 6.2 検索ロジック

1. ユーザーがクエリ入力(自然文)+ 都道府県(任意)
2. クエリをOpenAI Embeddings APIでベクトル化
3. pgvectorで cosine類似度 上位10件を抽出
4. 都道府県指定がある場合は事前フィルタ
5. 結果をカード形式で表示

### 6.3 「相談する」フローの詳細

1. 検索結果カード上で「相談する」クリック
2. URLパラメータで補助金IDを渡し、`/chat` 画面に遷移
3. チャット画面の初期メッセージとして補助金情報(タイトル・対象・金額・締切)を事前ペースト
4. ユーザーは追加質問を入力 → AI応答

これにより「補助金を見つける → その場で具体的に相談する」までの体験を一周検証できる。

-----

## 7. 削減できなかった/しなかった機能の根拠

### 7.1 「これは絶対に削れない」もの

| 機能 | 削れない理由 |
|--|--|
| 補助金RAG(自前構築) | テスト版の核心。ここを検証するために他を削っている |
| SSEストリーミング | チャットUXの体感差が大きく、後付けで実装方針が変わる可能性が高い |
| pgvector | 全文検索だとRAGの品質が出ず、テスト版の目的(RAG価値検証)が達成できない |
| 認証 | テスト4名でも個人別の挙動確認が必要 |
| RLS | 後付けの工数 > 最初から組み込む工数 |

### 7.2 「削った方が早いが残した」もの

| 機能 | 残す理由 |
|--|--|
| チャット履歴 | 履歴がないと「使えるサービス」感が出ない。フィードバックの質が落ちる |
| 都道府県絞り込み | RAG単体だと全国の補助金が混在して使いづらい |

-----

## 8. リスクと対応

### 8.1 主要リスク

| リスク | 影響 | 対応 |
|--|--|--|
| 補助金データ収集が想定より重い | 工程後ろ倒し | 経営者側でDay1から並行収集着手 |
| RAG検索精度が出ない | UX崩壊 | 埋め込みモデル変更/プロンプト再設計でDay14-16のバッファ吸収 |
| SSEストリーミング実装でハマる | チャット完成遅延 | Vercel公式サンプルを流用、Claude Code活用 |
| pgvectorの未経験 | DB構築遅延 | Supabase公式チュートリアル数時間で習得可能、Claude Code併用前提 |
| エンジニアの他案件割り込み | 全体工程崩壊 | テスト版期間中はフルコミット必須(事前合意) |
| 補助金データソースの法的問題 | データ削除・差し替え | 公開情報のみ・原典URL明示・引用扱い |
| OpenAI APIキー漏洩 | 経済的損失 | 環境変数のみで管理、クライアント露出禁止 |

### 8.2 18日達成の前提条件

- エンジニア2名がフルコミット(他案件の割り込みなし)
- 経営者が随時意思決定可能(Day1キックオフ + 日次15分スタンドアップ)
- 補助金データ収集は経営者またはアシスタント側で並行実施(エンジニア工数を圧縮)
- pgvector経験は未経験OK(学習1日見込み)
- Claude Code / Cursor 等のAIコーディング支援を活用前提

### 8.3 工程崩壊時のスコープ削減順序

遅延発生時、以下の順で削る:

1. 都道府県絞り込み(全国のみ表示で代用)
2. チャット履歴削除機能(閲覧のみ残す)
3. UI ポリッシュ(機能優先で見た目は最低限)
4. 「相談する」導線(検索とチャットを別画面のままにする)
5. チャット履歴(セッション単位だけにする)

逆に**絶対に削らない**:認証 / チャット本体 / 補助金検索 / SSE

-----

## 9. 完了判定(Day 18 ゴール)

### 9.1 機能完了判定

- [ ] メール認証で signup → login → logout が動く
- [ ] チャット画面で質問送信 → SSEストリーミング応答 → 履歴保存ができる
- [ ] チャット履歴サイドバーで過去会話を呼び出せる
- [ ] 補助金検索で「IT導入」「業務改善」等のクエリが意味的に正しい結果を返す
- [ ] 都道府県絞り込みが動く
- [ ] 検索結果から「相談する」でチャット画面に補助金情報がプリセットされる

### 9.2 品質判定

- [ ] チャット応答開始まで 2秒以内
- [ ] 補助金検索レスポンス 3秒以内
- [ ] RLS有効化済み(他ユーザーのチャット履歴が見えない)
- [ ] APIキーがクライアントに露出していない(DevTools確認)
- [ ] 主要ブラウザ(Chrome/Safari/Edge)で動作

### 9.3 受け入れ判定

- [ ] テストユーザー4名がアカウント作成→チャット→補助金検索を15分で一通り体験できる
- [ ] フィードバック収集の仕組み(Googleフォーム等)が動く

-----

## 10. テスト後の活用方針

### 10.1 フィードバック収集

テストユーザー4名(社内/グループ会社)から以下を収集:

- AIチャットの応答品質(飲食店向け文脈は機能しているか)
- 補助金検索の精度(欲しい補助金がちゃんと見つかるか)
- 「相談する」導線の自然さ
- UI/UXの違和感
- 「次に欲しい機能」の優先順位

### 10.2 本実装への反映

- テスト版で判明した実装上の難所(SSE / RAG / 認証)の対処パターンをドキュメント化
- ユーザーフィードバックを基に、本実装スコープを再評価
- 特に「テンプレ機能の必要性」をテストユーザーの声で判断

### 10.3 テスト版コードの扱い

- テスト版コードは**本実装の起点として継続利用**(捨て実装ではない)
- DB設計はテスト版時点で本実装と互換性を保つ
- ブランチ戦略:`main`(テスト版)→ 本実装で `develop` を切る

-----

## 11. 確認待ち事項(キックオフ前に解消)

以下は本ドキュメント作成時点で**未確定**。Day 1キックオフ前に意思決定が必要:

| # | 確認事項 | 確認先 |
|---|--|--|
| 1 | エンジニア2名のフルコミット可否(他案件の状況) | 経営者・エンジニア本人 |
| 2 | 補助金データ収集を経営者側で並行できるか(可なら2〜3日短縮可) | 経営者 |
| 3 | テスト対象ユーザー4名の確保(社内/グループ会社) | 経営者 |
| 4 | Day 1〜18のカレンダーブロック(連休・他予定との衝突確認) | 経営者・エンジニア |
| 5 | OpenAI API利用枠の確保(月予算想定) | 経営者 |
| 6 | Supabaseプロジェクト・Vercel(Hobby/Pro)契約状況 | 経営者 |
| 7 | 「既存資産」と呼ばれていた LMS / プロンプトサイト の実態確認(補助金RAG同様、実は未着手の可能性) | 既存資産担当者 |

### 7番の補足

『1ヶ月リリース実行計画書 v2.0』では補助金RAG・LMS・プロンプトサイト(140種)が「既存資産」とされていたが、補助金RAGは実態未着手であることが本ドキュメント作成過程で判明した。**LMSとプロンプトサイトについても同様の確認が必要**。これらの実態次第で本実装のスコープも大きく変わる。

-----

## 12. 改訂履歴

| 版 | 日付 | 改訂内容 |
|--|--|--|
| v1.0 | 2026年5月20日 | 初版作成。元計画書の30日全実装案からテスト版18日案への方針転換を反映 |

-----

**ドキュメント終了**

このドキュメントは、元計画書(『1ヶ月リリース実行計画書 v2_0.md』ほか)からの方針転換を含む新たな計画として作成されました。本実装着手前に、テスト版で実装難所とUX仮説を検証することを目的としています。
